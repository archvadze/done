<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <!-- SEO Meta Tags -->
    <title>@yield('title', 'Acumen Craft - Digital Art Gallery')</title>
    <meta name="description" content="@yield('description', 'Discover and share amazing digital artworks on Acumen Craft. Join our community of artists and art enthusiasts.')">
    <meta name="keywords" content="@yield('keywords', 'digital art, gallery, artwork, artists, community, creative, design')">
    <meta name="author" content="Acumen Craft">

    <!-- Open Graph / Facebook -->
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ url()->current() }}">
    <meta property="og:title" content="@yield('og_title', 'Acumen Craft - Digital Art Gallery')">
    <meta property="og:description" content="@yield('og_description', 'Discover and share amazing digital artworks on Acumen Craft.')">
    <meta property="og:image" content="@yield('og_image', asset('images/og-default.jpg'))">
    <meta property="og:site_name" content="Acumen Craft">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ url()->current() }}">
    <meta property="twitter:title" content="@yield('twitter_title', 'Acumen Craft - Digital Art Gallery')">
    <meta property="twitter:description" content="@yield('twitter_description', 'Discover and share amazing digital artworks on Acumen Craft.')">
    <meta property="twitter:image" content="@yield('twitter_image', asset('images/twitter-default.jpg'))">

    <!-- Canonical URL -->
    <link rel="canonical" href="@yield('canonical', url()->current())">

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon-16x16.png') }}">

    <!-- Preconnect to external domains for performance -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
    <link rel="stylesheet" href="{{ asset('css/custom.css') }}">
    <link rel="stylesheet" href="{{ asset('css/performance.css') }}">

    <!-- Navigation JavaScript -->
    <script src="{{ asset('js/nav.js') }}" defer></script>

    @stack('styles')

    <!-- Structured Data -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Acumen Craft",
        "description": "Digital Art Gallery and Community",
        "url": "{{ url('/') }}",
        "potentialAction": {
            "@type": "SearchAction",
            "target": "{{ url('/artworks') }}?search={search_term_string}",
            "query-input": "required name=search_term_string"
        }
    }
    </script>

    @stack('head')
</head>

<body class="bg-gray-50">
    @include('partials.nav')

    <!-- Main Content -->
    <main class="py-8" role="main">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            @yield('content')
        </div>
    </main>

    @include('partials.footer')

    <!-- Alpine.js for dropdowns -->
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>

    @stack('scripts')
</body>

</html>
